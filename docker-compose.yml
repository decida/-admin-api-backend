services:
  meuapp:
    container_name: decida-api-backend-compose
    build:
      context: .
      dockerfile: Dockerfile
    network_mode: host
    env_file:
      - .env
    user: "0:0"
    restart: unless-stopped
