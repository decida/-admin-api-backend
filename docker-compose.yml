version: "3.9"

services:
  meuapp:
    container_name: meuapp
    build:
      context: .
      dockerfile: Dockerfile
    network_mode: host
    env_file:
      - .env
    user: "0:0"
    restart: unless-stopped
